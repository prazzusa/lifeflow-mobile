import { View, Text, SafeAreaView } from 'react-native';

export default function HabitsDetailScreen() {
  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: '#0f0f1a', padding: 16 }}>
      <Text style={{ color: '#fff', fontSize: 24, fontWeight: '700' }}>Habits Detail</Text>
      <Text style={{ color: '#94a3b8', marginTop: 8 }}>HabitsDetailPage — coming in Block 5</Text>
    </SafeAreaView>
  );
}
